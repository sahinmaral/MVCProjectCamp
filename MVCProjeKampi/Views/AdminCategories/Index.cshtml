@using EntityLayer.Concrete
@using PagedList
@using PagedList.Mvc
@model IPagedList<Category>

@{
    ViewBag.Title = "Kategoriler";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

    string CheckCategoryStatus(bool categoryStatus)
    {
        switch (categoryStatus)
        {
            case true:
                return "Normal";
            case false:
                return "Silinmiş";
        }
    }
}

@*Sebebini anlamadığım şekilde kategori hariç
    diğer sayfalarda çoklu yenilemelerde sidebar küçülüyor.*@

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Kategori Adı</th>
        <th>Kategori İşlemleri</th>
        <th>Kategori Durumu</th>
        
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <th>@item.CategoryId</th>
            <td>@item.CategoryName</td>
            <td>@CheckCategoryStatus(item.CategoryStatus)</td>
            <td>
                <a href="/AdminCategories/DeleteCategory/@item.CategoryId" class="btn btn-danger">Sil</a>
                <a href="/AdminCategories/UpdateCategory/@item.CategoryId" class="btn btn-warning">Güncelle</a>
                <a href="../AdminHeadings/HeadingsByCategory/@item.CategoryId" class="btn btn-primary">Başlıklar</a>
            </td>
        </tr>
    }
</table>

<a href="../AdminCategories/AddCategory" class="btn btn-primary">Yeni Kategori Ekle</a>


    @Html.PagedListPager((IPagedList)Model,
            p => Url.Action("Index",
                new { p })
            ,
            new PagedListRenderOptions
            {
                LiElementClasses = new List<string>
                {
                    {"page-link"}
                },
                UlElementClasses = new List<string>
                {
                    {"pagination justify-content-center"}
                }
            }
            )
